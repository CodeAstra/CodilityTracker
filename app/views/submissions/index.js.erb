<% @submissions.each do |submission| %>
  SubmissionPoller.removeSubmissionFromPoll(<%= submission.id %>);
  var eleId = '#submission-<%= submission.id %>';
  $(eleId).replaceWith('<%= j render(submission) %>');
  highlightElement(eleId);
<% end %>